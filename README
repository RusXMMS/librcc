Library for autoconversion of charsets to/from UTF-8.

# Add rcd & db4 libs
# Add Gtk1 plugin
    class = charset class
    engine = auto engine
    selected - which is selected
    current - resolves default values

# ToDo
# XML files with additional languages